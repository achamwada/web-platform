#set ($response = $util.parseJson($input.path('$.errorMessage')))
{
  "statusCode" : $response.statusCode,
  "contentKey" : "$response.contentEntryKey",
  "message" : "$response.body",
  "correlationId" : "$response.awsRequestId"
}